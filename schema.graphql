type Todo {
  id: ID!
  text: String!
  completed: Boolean!
}

type Query {
  todo(id: ID!): Todo
  todos: [Todo]!
  search(filter: String!): [Todo]!
}

type Mutation {
  addTodo(text: String!): [Todo!]!
  completeTodo(id: ID!): String!
  deleteTodo(id: ID!): [Todo]!
}
